version: 0.2

phases:
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - mvn install
  post_build:
    commands:
      - echo "Packaging artifacts..."
      - mv **/LoginWebApp-*.war dist/LoginWebApp.war
artifacts:
  files:
    - dist/LoginWebApp.war
